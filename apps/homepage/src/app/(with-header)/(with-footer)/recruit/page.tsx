import {ActionContainer} from '@/app/(with-header)/(with-footer)/recruit/_containers/ActionContainer';
import {ContentContainer} from '@/app/(with-header)/(with-footer)/recruit/_containers/ContentContainer';

export default function RecruitPage() {
  return (
    <section>
      <ActionContainer />
      <ContentContainer />
    </section>
  );
}
